---
plugin: netbox.netbox.nb_inventory
api_endpoint: https://192.168.253.134
token: FZRvENMkPG8xXTKdP9ewxmGtSgLuN8xaAMHkvcgr
validate_certs: false

# FORCED BYPASS: Explicitly define resources so it doesn't "ask" the schema what exists
fetch_all: false
resources:
  - devices

# These must be false to stop the schema-based validation
validate_query_filters: false
config_context: false

query_filters:
  - status: "active"
  - cluster_id: 1

compose:
  ansible_host: primary_ip4.address.split('/')[0]
  primary_ip4: primary_ip4.address.split('/')[0]
