---
- name: Create Register and Start Nested ESXi VM in VMware Workstation
  hosts: localhost
  connection: local
  gather_facts: false

  vars:

    # ========================================================================
    # VM
    # ========================================================================

    vm_name: "ESXI-01"

    # Cygwin paths used by Ansible
    vm_dir_cygwin: "/cygdrive/d/ESXI/ESXI-01"
    vmx_file_cygwin: "/cygdrive/d/ESXI/ESXI-01/ESXI-01.vmx"
    vmdk_file_cygwin: "/cygdrive/d/ESXI/ESXI-01/ESXI-01.vmdk"
    vmware_log_cygwin: "/cygdrive/d/ESXI/ESXI-01/vmware.log"

    esxi_iso_cygwin: "/cygdrive/e/vmware/VMware-VMvisor-Installer-8.0U1-21495797.x86_64.iso"

    vmrun_cygwin: "/cygdrive/d/VM workstation/vmrun.exe"
    vdiskmanager_cygwin: "/cygdrive/d/VM workstation/vmware-vdiskmanager.exe"

    # Windows paths used by VMware executables / VMX
    vmx_file_windows: 'D:\ESXI\ESXI-01\ESXI-01.vmx'
    vmdk_file_windows: 'D:\ESXI\ESXI-01\ESXI-01.vmdk'
    esxi_iso_windows: 'E:\vmware\VMware-VMvisor-Installer-8.0U1-21495797.x86_64.iso'

    vmrun_windows: 'D:\VM workstation\vmrun.exe'
    vdiskmanager_windows: 'D:\VM workstation\vmware-vdiskmanager.exe'

    # ========================================================================
    # HARDWARE
    # ========================================================================

    vm_memory_mb: 24568
    vm_vcpu: 12
    vm_disk_gb: 2000

    virtual_hw_version: "19"
    guest_os: "vmkernel7"

    network_type: "bridged"
    network_name: "VMnet0"

    startup_wait: 15

  tasks:

    # ========================================================================
    # DISPLAY
    # ========================================================================

    - name: Display ESXi configuration
      ansible.builtin.debug:
        msg:
          - "============================================================"
          - "NESTED ESXi VM CONFIGURATION"
          - "============================================================"
          - "VM Name       : {{ vm_name }}"
          - "VMX           : {{ vmx_file_windows }}"
          - "VMDK          : {{ vmdk_file_windows }}"
          - "ESXi ISO      : {{ esxi_iso_windows }}"
          - "VMRUN         : {{ vmrun_windows }}"
          - "VDISKMANAGER  : {{ vdiskmanager_windows }}"
          - "RAM           : {{ vm_memory_mb }} MB"
          - "CPU           : {{ vm_vcpu }} vCPU"
          - "Disk          : {{ vm_disk_gb }} GB"
          - "Hardware Ver  : {{ virtual_hw_version }}"
          - "Guest OS      : {{ guest_os }}"
          - "Network       : {{ network_name }}"
          - "Firmware      : EFI"
          - "Nested VT     : ENABLED"
          - "PCIe Bridges  : ENABLED"
          - "USB           : DISABLED"
          - "xHCI          : DISABLED"
          - "Sound         : DISABLED"
          - "============================================================"


    # ========================================================================
    # VALIDATE ISO
    # ========================================================================

    - name: Check ESXi ISO
      ansible.builtin.stat:
        path: "{{ esxi_iso_cygwin }}"
      register: esxi_iso_stat

    - name: Fail if ESXi ISO does not exist
      ansible.builtin.fail:
        msg: |
          ============================================================
          ESXi ISO NOT FOUND
          ============================================================

          Cygwin path:
          {{ esxi_iso_cygwin }}

          Windows path:
          {{ esxi_iso_windows }}

          ============================================================
      when: not esxi_iso_stat.stat.exists


    # ========================================================================
    # VALIDATE VMWARE TOOLS
    # ========================================================================

    - name: Check vmrun.exe
      ansible.builtin.stat:
        path: "{{ vmrun_cygwin }}"
      register: vmrun_stat

    - name: Fail if vmrun.exe is missing
      ansible.builtin.fail:
        msg: |
          ============================================================
          VMWARE VMRUN.EXE NOT FOUND
          ============================================================

          Expected:
          {{ vmrun_windows }}

          ============================================================
      when: not vmrun_stat.stat.exists


    - name: Check vmware-vdiskmanager.exe
      ansible.builtin.stat:
        path: "{{ vdiskmanager_cygwin }}"
      register: vdiskmanager_stat

    - name: Fail if vdiskmanager.exe is missing
      ansible.builtin.fail:
        msg: |
          ============================================================
          VMWARE-VDISKMANAGER.EXE NOT FOUND
          ============================================================

          Expected:
          {{ vdiskmanager_windows }}

          ============================================================
      when: not vdiskmanager_stat.stat.exists


    # ========================================================================
    # CREATE VM DIRECTORY
    # ========================================================================

    - name: Create ESXi VM directory
      ansible.builtin.command:
        argv:
          - mkdir
          - -p
          - "{{ vm_dir_cygwin }}"
      changed_when: false


    # ========================================================================
    # CHECK CURRENT VM STATE
    # ========================================================================

    - name: Get current VMware running VM list
      ansible.builtin.command:
        argv:
          - "{{ vmrun_cygwin }}"
          - "-T"
          - "ws"
          - "list"
      register: vm_list_before
      changed_when: false
      failed_when: false

    - name: Determine VM running state
      ansible.builtin.set_fact:
        vm_running_before: >-
          {{
            vmx_file_windows | lower
            in
            (vm_list_before.stdout | lower)
          }}

    - name: Display current VM state
      ansible.builtin.debug:
        msg:
          - "VM: {{ vm_name }}"
          - "Running before configuration: {{ vm_running_before }}"


    # ========================================================================
    # POWER OFF MODE
    # ========================================================================

    - name: Power off ESXi VM
      ansible.builtin.command:
        argv:
          - "{{ vmrun_cygwin }}"
          - "-T"
          - "ws"
          - "stop"
          - "{{ vmx_file_windows }}"
          - "hard"
      register: poweroff_result
      failed_when: false
      changed_when: poweroff_result.rc == 0
      when:
        - power | default("on") | lower == "off"
        - vm_running_before | bool

    - name: Wait after power off
      ansible.builtin.pause:
        seconds: 5
      when:
        - power | default("on") | lower == "off"
        - vm_running_before | bool

    - name: Display power off result
      ansible.builtin.debug:
        msg:
          - "============================================================"
          - "VM POWER OFF RESULT"
          - "============================================================"
          - "Return code: {{ poweroff_result.rc | default(0) }}"
          - "STDOUT: {{ poweroff_result.stdout | default('VM already powered off') }}"
          - "STDERR: {{ poweroff_result.stderr | default('') }}"
          - "============================================================"
      when: power | default("on") | lower == "off"

    - name: End play after power off
      ansible.builtin.meta: end_play
      when: power | default("on") | lower == "off"


    # ========================================================================
    # STOP BEFORE REBUILD
    # ========================================================================

    - name: Stop VM before automatic VMX rebuild
      ansible.builtin.command:
        argv:
          - "{{ vmrun_cygwin }}"
          - "-T"
          - "ws"
          - "stop"
          - "{{ vmx_file_windows }}"
          - "hard"
      register: stop_before_rebuild
      failed_when: false
      changed_when: stop_before_rebuild.rc == 0
      when: vm_running_before | bool

    - name: Wait for VMware shutdown
      ansible.builtin.pause:
        seconds: 5
      when: vm_running_before | bool


    # ========================================================================
    # BACKUP EXISTING VMX
    # ========================================================================

    - name: Check existing VMX
      ansible.builtin.stat:
        path: "{{ vmx_file_cygwin }}"
      register: vmx_existing_stat

    - name: Backup existing VMX
      ansible.builtin.copy:
        src: "{{ vmx_file_cygwin }}"
        dest: "{{ vmx_file_cygwin }}.ansible-backup"
        remote_src: true
        mode: "0644"
      when: vmx_existing_stat.stat.exists


    # ========================================================================
    # REMOVE STALE LOCKS
    # ========================================================================

    - name: Remove stale VMware lock directories
      ansible.builtin.shell: |
        find "{{ vm_dir_cygwin }}" \
          -maxdepth 2 \
          -type d \
          -name "*.lck" \
          -exec rm -rf {} + 2>/dev/null || true
      args:
        executable: /bin/bash
      changed_when: false


    # ========================================================================
    # REMOVE STALE STATE
    # ========================================================================

    - name: Remove stale VMware state files
      ansible.builtin.shell: |
        rm -f "{{ vm_dir_cygwin }}"/*.vmem 2>/dev/null || true
        rm -f "{{ vm_dir_cygwin }}"/*.vmss 2>/dev/null || true
        rm -f "{{ vm_dir_cygwin }}"/*.vmpl 2>/dev/null || true
        rm -f "{{ vm_dir_cygwin }}"/*.vmsn 2>/dev/null || true
        rm -f "{{ vm_dir_cygwin }}"/*.scoreboard 2>/dev/null || true
        rm -f "{{ vm_dir_cygwin }}"/*.nvram 2>/dev/null || true
      args:
        executable: /bin/bash
      changed_when: false


    # ========================================================================
    # CHECK VMDK
    # ========================================================================

    - name: Check existing VMDK
      ansible.builtin.stat:
        path: "{{ vmdk_file_cygwin }}"
      register: vmdk_stat


    # ========================================================================
    # CREATE VMDK
    # ========================================================================

    - name: Create ESXi virtual disk
      ansible.builtin.command:
        argv:
          - "{{ vdiskmanager_cygwin }}"
          - "-c"
          - "-s"
          - "{{ vm_disk_gb }}GB"
          - "-a"
          - "lsilogic"
          - "-t"
          - "0"
          - "{{ vmdk_file_windows }}"
      register: vmdk_create
      when: not vmdk_stat.stat.exists
      changed_when: vmdk_create.rc == 0
      failed_when: vmdk_create.rc != 0


    - name: Display VMDK creation result
      ansible.builtin.debug:
        msg:
          - "VMDK return code: {{ vmdk_create.rc | default(0) }}"
          - "STDOUT: {{ vmdk_create.stdout | default('Existing VMDK preserved.') }}"
          - "STDERR: {{ vmdk_create.stderr | default('') }}"


    # ========================================================================
    # VERIFY VMDK
    # ========================================================================

    - name: Verify VMDK exists
      ansible.builtin.stat:
        path: "{{ vmdk_file_cygwin }}"
      register: vmdk_after

    - name: Fail if VMDK is missing
      ansible.builtin.fail:
        msg: |
          ============================================================
          VMDK CREATION FAILED
          ============================================================

          Expected:
          {{ vmdk_file_windows }}

          ============================================================
      when: not vmdk_after.stat.exists


    # ========================================================================
    # REBUILD CLEAN VMX
    # ========================================================================

    - name: Remove old VMX
      ansible.builtin.file:
        path: "{{ vmx_file_cygwin }}"
        state: absent


    - name: Create clean ESXi VMX
      ansible.builtin.copy:
        dest: "{{ vmx_file_cygwin }}"
        mode: "0644"
        content: |
          .encoding = "UTF-8"

          config.version = "8"
          virtualHW.version = "{{ virtual_hw_version }}"

          virtualHW.productCompatibility = "hosted"

          displayName = "{{ vm_name }}"
          guestOS = "{{ guest_os }}"

          firmware = "efi"
          uefi.secureBoot.enabled = "FALSE"

          memsize = "{{ vm_memory_mb }}"
          numvcpus = "{{ vm_vcpu }}"
          cpuid.coresPerSocket = "1"

          # ==========================================================
          # NESTED VIRTUALIZATION
          # ==========================================================

          vhv.enable = "TRUE"
          hypervisor.cpuid.v0 = "FALSE"

          monitor.virtual_exec = "hardware"
          monitor.virtual_mmu = "hardware"

          # ==========================================================
          # PCI BRIDGES
          # ==========================================================

          pciBridge0.present = "TRUE"
          pciBridge0.pciSlotNumber = "17"

          pciBridge1.present = "TRUE"
          pciBridge1.pciSlotNumber = "18"

          pciBridge2.present = "TRUE"
          pciBridge2.pciSlotNumber = "19"

          pciBridge3.present = "TRUE"
          pciBridge3.pciSlotNumber = "20"

          pciBridge4.present = "TRUE"
          pciBridge4.virtualDev = "pcieRootPort"
          pciBridge4.functions = "8"
          pciBridge4.pciSlotNumber = "21"

          pciBridge5.present = "TRUE"
          pciBridge5.virtualDev = "pcieRootPort"
          pciBridge5.functions = "8"
          pciBridge5.pciSlotNumber = "22"

          pciBridge6.present = "TRUE"
          pciBridge6.virtualDev = "pcieRootPort"
          pciBridge6.functions = "8"
          pciBridge6.pciSlotNumber = "23"

          pciBridge7.present = "TRUE"
          pciBridge7.virtualDev = "pcieRootPort"
          pciBridge7.functions = "8"
          pciBridge7.pciSlotNumber = "24"

          # ==========================================================
          # SCSI
          # ==========================================================

          scsi0.present = "TRUE"
          scsi0.virtualDev = "lsilogic"
          scsi0.pciSlotNumber = "160"

          scsi0:0.present = "TRUE"
          scsi0:0.fileName = "{{ vmdk_file_windows }}"
          scsi0:0.deviceType = "scsi-hardDisk"

          # ==========================================================
          # CD/DVD
          # ==========================================================

          ide1:0.present = "TRUE"
          ide1:0.deviceType = "cdrom-image"
          ide1:0.fileName = "{{ esxi_iso_windows }}"
          ide1:0.startConnected = "TRUE"

          # ==========================================================
          # NETWORK
          # ==========================================================

          ethernet0.present = "TRUE"
          ethernet0.connectionType = "{{ network_type }}"
          ethernet0.vnet = "{{ network_name }}"
          ethernet0.virtualDev = "e1000e"
          ethernet0.addressType = "generated"
          ethernet0.startConnected = "TRUE"
          ethernet0.pciSlotNumber = "1184"

          # ==========================================================
          # DISABLE UNNECESSARY DEVICES
          # ==========================================================

          usb.present = "FALSE"
          usb_xhci.present = "FALSE"
          ehci.present = "FALSE"

          sound.present = "FALSE"

          serial0.present = "FALSE"
          parallel0.present = "FALSE"

          floppy0.present = "FALSE"
          printer0.present = "FALSE"

          # ==========================================================
          # DISPLAY
          # ==========================================================

          svga.present = "TRUE"
          svga.autodetect = "TRUE"
          svga.vramSize = "134217728"

          # ==========================================================
          # POWER
          # ==========================================================

          powerType.powerOff = "soft"
          powerType.powerOn = "soft"
          powerType.suspend = "soft"
          powerType.reset = "soft"

          # ==========================================================
          # TOOLS
          # ==========================================================

          tools.upgrade.policy = "manual"
          tools.syncTime = "FALSE"


    # ========================================================================
    # VERIFY VMX
    # ========================================================================

    - name: Read generated VMX
      ansible.builtin.slurp:
        src: "{{ vmx_file_cygwin }}"
      register: vmx_slurp

    - name: Decode VMX
      ansible.builtin.set_fact:
        vmx_text: "{{ vmx_slurp.content | b64decode }}"


    - name: Verify PCI bridges
      ansible.builtin.assert:
        that:
          - "'pciBridge0.present = \"TRUE\"' in vmx_text"
          - "'pciBridge4.present = \"TRUE\"' in vmx_text"
          - "'pciBridge4.virtualDev = \"pcieRootPort\"' in vmx_text"
          - "'pciBridge7.present = \"TRUE\"' in vmx_text"
        fail_msg: "PCIe bridge configuration is missing."
        success_msg: "PCIe bridge configuration verified."


    - name: Verify SCSI configuration
      ansible.builtin.assert:
        that:
          - "'scsi0.present = \"TRUE\"' in vmx_text"
          - "'scsi0.virtualDev = \"lsilogic\"' in vmx_text"
          - "'scsi0:0.present = \"TRUE\"' in vmx_text"
        fail_msg: "SCSI configuration is missing."
        success_msg: "SCSI configuration verified."


    - name: Verify Ethernet configuration
      ansible.builtin.assert:
        that:
          - "'ethernet0.present = \"TRUE\"' in vmx_text"
          - "'ethernet0.virtualDev = \"e1000e\"' in vmx_text"
        fail_msg: "Ethernet configuration is missing."
        success_msg: "Ethernet configuration verified."


    - name: Verify nested virtualization
      ansible.builtin.assert:
        that:
          - "'vhv.enable = \"TRUE\"' in vmx_text"
        fail_msg: "Nested virtualization is disabled."
        success_msg: "Nested virtualization enabled."


    - name: Verify hardware version
      ansible.builtin.assert:
        that:
          - ('virtualHW.version = "' ~ virtual_hw_version ~ '"') in vmx_text
        fail_msg: "Incorrect hardware version."
        success_msg: "Hardware version verified."


    # ========================================================================
    # REMOVE OLD LOG
    # ========================================================================

    - name: Remove old VMware log
      ansible.builtin.file:
        path: "{{ vmware_log_cygwin }}"
        state: absent


    # ========================================================================
    # REGISTER VM IN VMWARE WORKSTATION LIBRARY
    # ========================================================================

    - name: Get VMware registered VM list before registration
      ansible.builtin.command:
        argv:
          - "{{ vmrun_cygwin }}"
          - "-T"
          - "ws"
          - "listRegisteredVM"
      register: registered_vms_before
      changed_when: false
      failed_when: false


    - name: Display current registered VMware VMs
      ansible.builtin.debug:
        msg: "{{ registered_vms_before.stdout_lines | default([]) }}"


    - name: Register ESXi VM in VMware Workstation Library
      ansible.builtin.command:
        argv:
          - "{{ vmrun_cygwin }}"
          - "-T"
          - "ws"
          - "register"
          - "{{ vmx_file_windows }}"
      register: register_esxi
      changed_when: register_esxi.rc == 0
      failed_when: false
      when: vmx_file_windows | lower not in (registered_vms_before.stdout | lower)


    - name: Display VMware Library registration result
      ansible.builtin.debug:
        msg:
          - "============================================================"
          - "VMWARE WORKSTATION LIBRARY REGISTRATION"
          - "============================================================"
          - "VM Name: {{ vm_name }}"
          - "VMX    : {{ vmx_file_windows }}"
          - "Return : {{ register_esxi.rc | default(0) }}"
          - "STDOUT : {{ register_esxi.stdout | default('Already registered') }}"
          - "STDERR : {{ register_esxi.stderr | default('') }}"
          - "============================================================"


    # ========================================================================
    # VERIFY REGISTRATION
    # ========================================================================

    - name: Get VMware registered VM list after registration
      ansible.builtin.command:
        argv:
          - "{{ vmrun_cygwin }}"
          - "-T"
          - "ws"
          - "listRegisteredVM"
      register: registered_vms_after
      changed_when: false
      failed_when: false


    - name: Determine whether ESXi VM is registered
      ansible.builtin.set_fact:
        vm_registered: >-
          {{
            vmx_file_windows | lower
            in
            (registered_vms_after.stdout | lower)
          }}


    - name: Fail if ESXi VM is not registered
      ansible.builtin.fail:
        msg: |
          ============================================================
          ESXI VM REGISTRATION FAILED
          ============================================================

          VMX:
          {{ vmx_file_windows }}

          Registered VMs:

          {{ registered_vms_after.stdout }}

          ============================================================
      when: not vm_registered


    - name: Display successful VMware Library registration
      ansible.builtin.debug:
        msg:
          - "============================================================"
          - "VMWARE WORKSTATION LIBRARY REGISTRATION SUCCESS"
          - "============================================================"
          - "VM Name : {{ vm_name }}"
          - "VMX     : {{ vmx_file_windows }}"
          - "Library : REGISTERED"
          - "============================================================"


    # ========================================================================
    # START VM
    # ========================================================================

    - name: Start Nested ESXi VM
      ansible.builtin.command:
        argv:
          - "{{ vmrun_cygwin }}"
          - "-T"
          - "ws"
          - "start"
          - "{{ vmx_file_windows }}"
          - "nogui"
      register: vm_start
      changed_when: vm_start.rc == 0
      failed_when: false


    - name: Display VMware start result
      ansible.builtin.debug:
        msg:
          - "============================================================"
          - "VMWARE START RESULT"
          - "============================================================"
          - "Return code: {{ vm_start.rc }}"
          - "STDOUT:"
          - "{{ vm_start.stdout | default('') }}"
          - "STDERR:"
          - "{{ vm_start.stderr | default('') }}"
          - "============================================================"


    # ========================================================================
    # WAIT
    # ========================================================================

    - name: Wait for VMware startup
      ansible.builtin.pause:
        seconds: "{{ startup_wait }}"
      when: vm_start.rc == 0


    # ========================================================================
    # VERIFY RUNNING VM
    # ========================================================================

    - name: Get VMware running VM list
      ansible.builtin.command:
        argv:
          - "{{ vmrun_cygwin }}"
          - "-T"
          - "ws"
          - "list"
      register: vmrun_after
      changed_when: false
      failed_when: false


    - name: Determine final running state
      ansible.builtin.set_fact:
        vm_running_after: >-
          {{
            vmx_file_windows | lower
            in
            (vmrun_after.stdout | lower)
          }}


    - name: Display final ESXi status
      ansible.builtin.debug:
        msg:
          - "============================================================"
          - "FINAL ESXi VM STATUS"
          - "============================================================"
          - "VM Name   : {{ vm_name }}"
          - "VMX       : {{ vmx_file_windows }}"
          - "Registered: {{ vm_registered }}"
          - "Running   : {{ vm_running_after }}"
          - "============================================================"


    # ========================================================================
    # SUCCESS
    # ========================================================================

    - name: Display successful startup
      ansible.builtin.debug:
        msg:
          - "============================================================"
          - "SUCCESS - ESXi VM IS REGISTERED AND RUNNING"
          - "============================================================"
          - "VM Name   : {{ vm_name }}"
          - "VMX       : {{ vmx_file_windows }}"
          - "Registered: YES"
          - "Running   : YES"
          - "RAM       : {{ vm_memory_mb }} MB"
          - "CPU       : {{ vm_vcpu }}"
          - "Disk      : {{ vm_disk_gb }} GB"
          - "Network   : {{ network_name }}"
          - "============================================================"
      when: vm_running_after | bool


    # ========================================================================
    # FAILURE DIAGNOSTICS
    # ========================================================================

    - name: Check VMware log after startup failure
      ansible.builtin.stat:
        path: "{{ vmware_log_cygwin }}"
      register: vmware_log_stat
      when: not vm_running_after | bool


    - name: Read current VMware log
      ansible.builtin.shell: |
        tail -150 "{{ vmware_log_cygwin }}"
      args:
        executable: /bin/bash
      register: vmware_log_tail
      changed_when: false
      failed_when: false
      when:
        - not vm_running_after | bool
        - vmware_log_stat.stat.exists


    - name: Extract current VMware errors
      ansible.builtin.shell: |
        grep -iE \
        "error|failed|failure|no pcIe|pci|slot|ethernet|scsi|rootPort|power on failed|unable to allocate" \
        "{{ vmware_log_cygwin }}" |
        tail -80
      args:
        executable: /bin/bash
      register: vmware_errors
      changed_when: false
      failed_when: false
      when:
        - not vm_running_after | bool
        - vmware_log_stat.stat.exists


    - name: Fail with VMware diagnostics
      ansible.builtin.fail:
        msg: |
          ============================================================
          FAILED TO START NESTED ESXi VM
          ============================================================

          VM:
          {{ vm_name }}

          VMX:
          {{ vmx_file_windows }}

          Library Registered:
          {{ vm_registered }}

          vmrun return code:
          {{ vm_start.rc }}

          vmrun stdout:
          {{ vm_start.stdout | default('') }}

          vmrun stderr:
          {{ vm_start.stderr | default('') }}

          ------------------------------------------------------------
          CURRENT VMWARE ERRORS
          ------------------------------------------------------------

          {{ vmware_errors.stdout | default('No filtered errors found.') }}

          ------------------------------------------------------------
          LAST VMWARE LOG
          ------------------------------------------------------------

          {{ vmware_log_tail.stdout | default('VMware log unavailable.') }}

          ------------------------------------------------------------
          VMX BACKUP
          ------------------------------------------------------------

          {{ vmx_file_cygwin }}.ansible-backup

          ============================================================
      when: not vm_running_after | bool
