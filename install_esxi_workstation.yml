---
- name: Automated Nested ESXi 8.0U1 Installation in VMware Workstation
  hosts: localhost
  connection: local
  gather_facts: false

  vars:

    # ========================================================================
    # ESXi VM
    # ========================================================================

    vm_name: "ESXI-01"

    vm_dir_cygwin: "/cygdrive/d/ESXI/ESXI-01"
    vmx_file_cygwin: "/cygdrive/d/ESXI/ESXI-01/ESXI-01.vmx"
    vmdk_file_cygwin: "/cygdrive/d/ESXI/ESXI-01/ESXI-01.vmdk"
    vmware_log_cygwin: "/cygdrive/d/ESXI/ESXI-01/vmware.log"

    vmx_file_windows: 'D:\ESXI\ESXI-01\ESXI-01.vmx'
    vmdk_file_windows: 'D:\ESXI\ESXI-01\ESXI-01.vmdk'

    # ========================================================================
    # ESXi ISO
    #
    # CUSTOMIZED UNATTENDED ISO
    # Moved to:
    # E:\vmware\ESXi-8.0.1-auto.iso
    # ========================================================================

    esxi_iso_cygwin: "/cygdrive/e/vmware/ESXi-8.0.1-auto.iso"
    esxi_iso_windows: 'E:\vmware\ESXi-8.0.1-auto.iso'

    # Original ISO
    original_iso_cygwin: "/cygdrive/e/vmware/VMware-VMvisor-Installer-8.0U1-21495797.x86_64.iso"

    # ========================================================================
    # VMware Workstation
    # ========================================================================

    vmrun_cygwin: "/cygdrive/d/VM workstation/vmrun.exe"
    vdiskmanager_cygwin: "/cygdrive/d/VM workstation/vmware-vdiskmanager.exe"
    vmware_cygwin: "/cygdrive/d/VM workstation/vmware.exe"

    vmrun_windows: 'D:\VM workstation\vmrun.exe'
    vdiskmanager_windows: 'D:\VM workstation\vmware-vdiskmanager.exe'
    vmware_windows: 'D:\VM workstation\vmware.exe'

    # ========================================================================
    # VM HARDWARE
    # ========================================================================

    vm_memory_mb: 24568
    vm_vcpu: 12
    vm_disk_gb: 2000

    virtual_hw_version: "19"
    guest_os: "vmkernel7"

    # ========================================================================
    # NETWORK
    #
    # IMPORTANT:
    # VMware Workstation UI should show:
    #
    # Custom:
    # Specific virtual network
    # VMnet0
    # ========================================================================

    network_type: "custom"
    network_name: "VMnet0"

    # ========================================================================
    # INSTALLATION TIMING
    # ========================================================================

    esxi_install_wait_seconds: 420
    vm_start_wait_seconds: 20
    vm_shutdown_wait_seconds: 10

    # ========================================================================
    # LOCAL TEMP
    # ========================================================================

    ansible_remote_tmp: "/tmp/.ansible-vmware"

  tasks:

    # ========================================================================
    # DISPLAY CONFIGURATION
    # ========================================================================

    - name: Display ESXi automation configuration
      ansible.builtin.debug:
        msg:
          - "============================================================"
          - "AUTOMATED NESTED ESXi INSTALLATION"
          - "============================================================"
          - "VM Name          : {{ vm_name }}"
          - "VMX              : {{ vmx_file_windows }}"
          - "VMDK             : {{ vmdk_file_windows }}"
          - "CUSTOM ISO       : {{ esxi_iso_windows }}"
          - "ORIGINAL ISO     : {{ original_iso_cygwin }}"
          - "RAM              : {{ vm_memory_mb }} MB"
          - "CPU              : {{ vm_vcpu }} vCPU"
          - "Disk             : {{ vm_disk_gb }} GB"
          - "Hardware Version : {{ virtual_hw_version }}"
          - "Firmware         : EFI"
          - "Nested VT        : ENABLED"
          - "Network Type     : {{ network_type }}"
          - "Network          : {{ network_name }}"
          - "USB              : DISABLED"
          - "xHCI             : DISABLED"
          - "Sound            : DISABLED"
          - "Serial           : DISABLED"
          - "Parallel         : DISABLED"
          - "Floppy           : DISABLED"
          - "Install Wait     : {{ esxi_install_wait_seconds }} seconds"
          - "============================================================"

    # ========================================================================
    # CREATE TEMP
    # ========================================================================

    - name: Create Ansible local temporary directory
      ansible.builtin.shell: |
        mkdir -p "{{ ansible_remote_tmp }}"
        chmod 700 "{{ ansible_remote_tmp }}"
      args:
        executable: /bin/bash
      changed_when: false

    # ========================================================================
    # VALIDATE ORIGINAL ISO
    # ========================================================================

    - name: Check original ESXi ISO exists
      ansible.builtin.stat:
        path: "{{ original_iso_cygwin }}"
      register: original_iso_stat

    - name: Fail if original ESXi ISO is missing
      ansible.builtin.fail:
        msg: |
          ============================================================
          ORIGINAL ESXi ISO NOT FOUND
          ============================================================

          Expected:
          {{ original_iso_cygwin }}

          ============================================================
      when: not original_iso_stat.stat.exists

    # ========================================================================
    # VALIDATE CUSTOM ISO
    # ========================================================================

    - name: Check customized ESXi ISO exists
      ansible.builtin.stat:
        path: "{{ esxi_iso_cygwin }}"
      register: custom_iso_stat

    - name: Fail if customized ESXi ISO is missing
      ansible.builtin.fail:
        msg: |
          ============================================================
          CUSTOMIZED ESXi ISO NOT FOUND
          ============================================================

          Expected:
          {{ esxi_iso_cygwin }}

          Windows:
          {{ esxi_iso_windows }}

          Make sure this file exists on E:\vmware.

          ============================================================
      when: not custom_iso_stat.stat.exists

    - name: Display ISO validation
      ansible.builtin.debug:
        msg:
          - "Original ISO : {{ original_iso_cygwin }}"
          - "Custom ISO   : {{ esxi_iso_cygwin }}"
          - "Custom ISO   : FOUND"

    # ========================================================================
    # VALIDATE VMware BINARIES
    # ========================================================================

    - name: Check vmrun.exe
      ansible.builtin.stat:
        path: "{{ vmrun_cygwin }}"
      register: vmrun_stat

    - name: Fail if vmrun.exe is missing
      ansible.builtin.fail:
        msg: |
          VMware vmrun.exe not found:

          {{ vmrun_windows }}
      when: not vmrun_stat.stat.exists

    - name: Check vmware-vdiskmanager.exe
      ansible.builtin.stat:
        path: "{{ vdiskmanager_cygwin }}"
      register: vdiskmanager_stat

    - name: Fail if vmware-vdiskmanager.exe is missing
      ansible.builtin.fail:
        msg: |
          VMware vmware-vdiskmanager.exe not found:

          {{ vdiskmanager_windows }}
      when: not vdiskmanager_stat.stat.exists

    - name: Check vmware.exe
      ansible.builtin.stat:
        path: "{{ vmware_cygwin }}"
      register: vmware_stat

    - name: Fail if vmware.exe is missing
      ansible.builtin.fail:
        msg: |
          VMware Workstation executable not found:

          {{ vmware_windows }}
      when: not vmware_stat.stat.exists

    # ========================================================================
    # CREATE VM DIRECTORY
    # ========================================================================

    - name: Create ESXi VM directory
      ansible.builtin.file:
        path: "{{ vm_dir_cygwin }}"
        state: directory
        mode: "0755"

    # ========================================================================
    # GET CURRENT VM STATE
    # ========================================================================

    - name: Get running VMware virtual machines
      ansible.builtin.command:
        argv:
          - "{{ vmrun_cygwin }}"
          - "-T"
          - "ws"
          - "list"
      register: vmrun_list_before
      changed_when: false
      failed_when: false

    - name: Determine whether ESXi VM is running
      ansible.builtin.set_fact:
        vm_running_before: >-
          {{
            vmx_file_windows | lower
            in
            (vmrun_list_before.stdout | lower)
          }}

    - name: Display current VM status
      ansible.builtin.debug:
        msg:
          - "VM Name : {{ vm_name }}"
          - "Running : {{ vm_running_before }}"

    # ========================================================================
    # STOP EXISTING VM
    # ========================================================================

    - name: Stop existing ESXi VM
      ansible.builtin.command:
        argv:
          - "{{ vmrun_cygwin }}"
          - "-T"
          - "ws"
          - "stop"
          - "{{ vmx_file_windows }}"
          - "hard"
      register: stop_existing
      failed_when: false
      changed_when: stop_existing.rc == 0
      when: vm_running_before | bool

    - name: Wait for existing ESXi VM shutdown
      ansible.builtin.pause:
        seconds: "{{ vm_shutdown_wait_seconds }}"
      when: vm_running_before | bool

    # ========================================================================
    # REMOVE STALE LOCKS
    # ========================================================================

    - name: Remove stale VMware lock directories
      ansible.builtin.shell: |
        find "{{ vm_dir_cygwin }}" \
          -maxdepth 2 \
          -type d \
          -name "*.lck" \
          -exec rm -rf {} + 2>/dev/null || true
      args:
        executable: /bin/bash
      changed_when: false

    # ========================================================================
    # REMOVE STALE STATE
    # ========================================================================

    - name: Remove stale VMware state files
      ansible.builtin.shell: |
        rm -f "{{ vm_dir_cygwin }}"/*.vmem 2>/dev/null || true
        rm -f "{{ vm_dir_cygwin }}"/*.vmss 2>/dev/null || true
        rm -f "{{ vm_dir_cygwin }}"/*.vmsn 2>/dev/null || true
        rm -f "{{ vm_dir_cygwin }}"/*.vmpl 2>/dev/null || true
        rm -f "{{ vm_dir_cygwin }}"/*.scoreboard 2>/dev/null || true
      args:
        executable: /bin/bash
      changed_when: false

    # ========================================================================
    # CHECK EXISTING VMX
    # ========================================================================

    - name: Check existing VMX
      ansible.builtin.stat:
        path: "{{ vmx_file_cygwin }}"
      register: vmx_existing_stat

    - name: Backup existing VMX
      ansible.builtin.copy:
        src: "{{ vmx_file_cygwin }}"
        dest: "{{ vmx_file_cygwin }}.pre-install-backup"
        remote_src: true
        mode: "0644"
      when: vmx_existing_stat.stat.exists

    # ========================================================================
    # CHECK EXISTING VMDK
    # ========================================================================

    - name: Check existing VMDK
      ansible.builtin.stat:
        path: "{{ vmdk_file_cygwin }}"
      register: vmdk_stat

    # ========================================================================
    # CREATE VMDK
    # ========================================================================

    - name: Create ESXi virtual disk
      ansible.builtin.command:
        argv:
          - "{{ vdiskmanager_cygwin }}"
          - "-c"
          - "-s"
          - "{{ vm_disk_gb }}GB"
          - "-a"
          - "lsilogic"
          - "-t"
          - "0"
          - "{{ vmdk_file_windows }}"
      register: vmdk_create
      changed_when: vmdk_create.rc == 0
      failed_when: vmdk_create.rc != 0
      when: not vmdk_stat.stat.exists

    - name: Display virtual disk result
      ansible.builtin.debug:
        msg:
          - "============================================================"
          - "VIRTUAL DISK"
          - "============================================================"
          - "Disk : {{ vmdk_file_windows }}"
          - "Size : {{ vm_disk_gb }} GB"
          - "Status:"
          - "{{ vmdk_create.stdout | default('Existing VMDK preserved.') }}"
          - "============================================================"

    # ========================================================================
    # VERIFY VMDK
    # ========================================================================

    - name: Verify VMDK exists
      ansible.builtin.stat:
        path: "{{ vmdk_file_cygwin }}"
      register: vmdk_after

    - name: Fail if VMDK does not exist
      ansible.builtin.fail:
        msg: |
          VMDK was not created:

          {{ vmdk_file_windows }}
      when: not vmdk_after.stat.exists

    # ========================================================================
    # CREATE INSTALLATION VMX
    # ========================================================================

    - name: Remove old VMX before installation configuration
      ansible.builtin.file:
        path: "{{ vmx_file_cygwin }}"
        state: absent

    - name: Create installation VMX
      ansible.builtin.copy:
        dest: "{{ vmx_file_cygwin }}"
        mode: "0644"
        content: |
          .encoding = "UTF-8"

          config.version = "8"
          virtualHW.version = "{{ virtual_hw_version }}"
          virtualHW.productCompatibility = "hosted"

          displayName = "{{ vm_name }}"
          guestOS = "{{ guest_os }}"

          firmware = "efi"
          uefi.secureBoot.enabled = "FALSE"

          memsize = "{{ vm_memory_mb }}"
          numvcpus = "{{ vm_vcpu }}"
          cpuid.coresPerSocket = "1"

          # ==========================================================
          # NESTED VIRTUALIZATION
          # ==========================================================

          vhv.enable = "TRUE"
          hypervisor.cpuid.v0 = "FALSE"

          monitor.virtual_exec = "hardware"
          monitor.virtual_mmu = "hardware"

          # ==========================================================
          # PCIe BRIDGES
          # ==========================================================

          pciBridge0.present = "TRUE"
          pciBridge0.pciSlotNumber = "17"

          pciBridge1.present = "TRUE"
          pciBridge1.pciSlotNumber = "18"

          pciBridge2.present = "TRUE"
          pciBridge2.pciSlotNumber = "19"

          pciBridge3.present = "TRUE"
          pciBridge3.pciSlotNumber = "20"

          pciBridge4.present = "TRUE"
          pciBridge4.virtualDev = "pcieRootPort"
          pciBridge4.functions = "8"
          pciBridge4.pciSlotNumber = "21"

          pciBridge5.present = "TRUE"
          pciBridge5.virtualDev = "pcieRootPort"
          pciBridge5.functions = "8"
          pciBridge5.pciSlotNumber = "22"

          pciBridge6.present = "TRUE"
          pciBridge6.virtualDev = "pcieRootPort"
          pciBridge6.functions = "8"
          pciBridge6.pciSlotNumber = "23"

          pciBridge7.present = "TRUE"
          pciBridge7.virtualDev = "pcieRootPort"
          pciBridge7.functions = "8"
          pciBridge7.pciSlotNumber = "24"

          # ==========================================================
          # SCSI CONTROLLER
          # ==========================================================

          scsi0.present = "TRUE"
          scsi0.virtualDev = "lsilogic"
          scsi0.pciSlotNumber = "160"

          scsi0:0.present = "TRUE"
          scsi0:0.fileName = "{{ vmdk_file_windows }}"
          scsi0:0.deviceType = "scsi-hardDisk"

          # ==========================================================
          # CUSTOMIZED ESXi ISO
          #
          # EXACT LOCATION:
          # E:\vmware\ESXi-8.0.1-auto.iso
          # ==========================================================

          ide1:0.present = "TRUE"
          ide1:0.deviceType = "cdrom-image"
          ide1:0.fileName = "{{ esxi_iso_windows }}"
          ide1:0.startConnected = "TRUE"

          # ==========================================================
          # NETWORK
          #
          # IMPORTANT:
          # Custom / VMnet0
          # ==========================================================

          ethernet0.present = "TRUE"
          ethernet0.connectionType = "custom"
          ethernet0.vnet = "VMnet0"
          ethernet0.virtualDev = "e1000e"
          ethernet0.addressType = "generated"
          ethernet0.startConnected = "TRUE"
          ethernet0.pciSlotNumber = "1184"

          # ==========================================================
          # DISABLE UNNECESSARY DEVICES
          # ==========================================================

          usb.present = "FALSE"
          usb_xhci.present = "FALSE"
          ehci.present = "FALSE"

          sound.present = "FALSE"

          serial0.present = "FALSE"
          parallel0.present = "FALSE"
          floppy0.present = "FALSE"
          printer0.present = "FALSE"

          # ==========================================================
          # DISPLAY
          # ==========================================================

          svga.present = "TRUE"
          svga.autodetect = "TRUE"
          svga.vramSize = "134217728"

          # ==========================================================
          # BOOT FROM CUSTOMIZED ISO
          # ==========================================================

          bios.bootDeviceClasses = "cd"

          # ==========================================================
          # POWER
          # ==========================================================

          powerType.powerOff = "soft"
          powerType.powerOn = "soft"
          powerType.suspend = "soft"
          powerType.reset = "soft"

          # ==========================================================
          # TOOLS
          # ==========================================================

          tools.upgrade.policy = "manual"
          tools.syncTime = "FALSE"

    # ========================================================================
    # VERIFY INSTALLATION VMX
    # ========================================================================

    - name: Read installation VMX
      ansible.builtin.slurp:
        src: "{{ vmx_file_cygwin }}"
      register: installation_vmx_slurp

    - name: Set installation VMX content
      ansible.builtin.set_fact:
        installation_vmx_text: >-
          {{
            installation_vmx_slurp.content
            | b64decode
          }}

    - name: Verify installation VMX
      ansible.builtin.assert:
        that:
          - ('virtualHW.version = "' ~ virtual_hw_version ~ '"') in installation_vmx_text
          - 'firmware = "efi"' in installation_vmx_text
          - 'vhv.enable = "TRUE"' in installation_vmx_text
          - 'ethernet0.connectionType = "custom"' in installation_vmx_text
          - 'ethernet0.vnet = "VMnet0"' in installation_vmx_text
          - 'scsi0.present = "TRUE"' in installation_vmx_text
          - 'ide1:0.present = "TRUE"' in installation_vmx_text
          - 'ide1:0.startConnected = "TRUE"' in installation_vmx_text
          - 'bios.bootDeviceClasses = "cd"' in installation_vmx_text
        fail_msg: "Installation VMX validation failed."
        success_msg: "Installation VMX validated successfully."

    - name: Display installation VMX summary
      ansible.builtin.debug:
        msg:
          - "============================================================"
          - "INSTALLATION VMX READY"
          - "============================================================"
          - "Hardware Version : {{ virtual_hw_version }}"
          - "Firmware         : EFI"
          - "Nested VT        : TRUE"
          - "Network          : CUSTOM / VMnet0"
          - "CD               : CONNECTED"
          - "Boot Device      : CD"
          - "Kickstart ISO    : {{ esxi_iso_windows }}"
          - "============================================================"

    # ========================================================================
    # REMOVE OLD LOG
    # ========================================================================

    - name: Remove old VMware log
      ansible.builtin.file:
        path: "{{ vmware_log_cygwin }}"
        state: absent

    # ========================================================================
    # START INSTALLATION
    # ========================================================================

    - name: Start ESXi unattended installation
      ansible.builtin.command:
        argv:
          - powershell.exe
          - "-NoProfile"
          - "-NonInteractive"
          - "-Command"
          - >-
            Start-Process
            -FilePath "{{ vmware_windows }}"
            -ArgumentList @("-x","{{ vmx_file_windows }}")
      register: installation_start
      changed_when: true
      failed_when: false

    - name: Display installation launch result
      ansible.builtin.debug:
        msg:
          - "============================================================"
          - "ESXi INSTALLATION STARTED"
          - "============================================================"
          - "VM      : {{ vm_name }}"
          - "VMX     : {{ vmx_file_windows }}"
          - "ISO     : {{ esxi_iso_windows }}"
          - "Return  : {{ installation_start.rc }}"
          - "============================================================"

    # ========================================================================
    # INITIAL WAIT
    # ========================================================================

    - name: Wait for ESXi installer to start
      ansible.builtin.pause:
        seconds: "{{ vm_start_wait_seconds }}"

    # ========================================================================
    # VERIFY INSTALL VM RUNNING
    # ========================================================================

    - name: Check VM during installation
      ansible.builtin.command:
        argv:
          - "{{ vmrun_cygwin }}"
          - "-T"
          - "ws"
          - "list"
      register: install_vm_list
      changed_when: false
      failed_when: false

    - name: Determine installation VM running state
      ansible.builtin.set_fact:
        installation_vm_running: >-
          {{
            vmx_file_windows | lower
            in
            (install_vm_list.stdout | lower)
          }}

    - name: Fail if installation VM did not start
      ansible.builtin.fail:
        msg: |
          ============================================================
          ESXi INSTALLATION VM DID NOT START
          ============================================================

          VMX:
          {{ vmx_file_windows }}

          VMware list:
          {{ install_vm_list.stdout }}

          ============================================================
      when: not installation_vm_running

    - name: Display installer running state
      ansible.builtin.debug:
        msg:
          - "ESXi installer VM is running."
          - "Waiting {{ esxi_install_wait_seconds }} seconds"
          - "for unattended installation and reboot."

    # ========================================================================
    # WAIT FOR INSTALLATION
    # ========================================================================

    - name: Wait for unattended ESXi installation
      ansible.builtin.pause:
        seconds: "{{ esxi_install_wait_seconds }}"

    # ========================================================================
    # CHECK VM AFTER INSTALLATION
    # ========================================================================

    - name: Check VM after installation wait
      ansible.builtin.command:
        argv:
          - "{{ vmrun_cygwin }}"
          - "-T"
          - "ws"
          - "list"
      register: install_vm_list_after_wait
      changed_when: false
      failed_when: false

    - name: Determine VM state after installation wait
      ansible.builtin.set_fact:
        vm_running_after_install: >-
          {{
            vmx_file_windows | lower
            in
            (install_vm_list_after_wait.stdout | lower)
          }}

    - name: Display state after installation wait
      ansible.builtin.debug:
        msg:
          - "============================================================"
          - "POST-INSTALLATION STATE"
          - "============================================================"
          - "VM Running : {{ vm_running_after_install }}"
          - "============================================================"

    # ========================================================================
    # STOP AFTER INSTALLATION
    # ========================================================================

    - name: Stop VM after installation
      ansible.builtin.command:
        argv:
          - "{{ vmrun_cygwin }}"
          - "-T"
          - "ws"
          - "stop"
          - "{{ vmx_file_windows }}"
          - "hard"
      register: post_install_stop
      failed_when: false
      changed_when: post_install_stop.rc == 0
      when: vm_running_after_install | bool

    - name: Wait for post-installation shutdown
      ansible.builtin.pause:
        seconds: "{{ vm_shutdown_wait_seconds }}"

    # ========================================================================
    # BACKUP INSTALLATION VMX
    # ========================================================================

    - name: Backup installation VMX
      ansible.builtin.copy:
        src: "{{ vmx_file_cygwin }}"
        dest: "{{ vmx_file_cygwin }}.installed-backup"
        remote_src: true
        mode: "0644"

    # ========================================================================
    # FINAL VMX
    #
    # CD disconnected
    # Normal disk boot
    # VMnet0 retained
    # ========================================================================

    - name: Create final installed ESXi VMX
      ansible.builtin.copy:
        dest: "{{ vmx_file_cygwin }}"
        mode: "0644"
        content: |
          .encoding = "UTF-8"

          config.version = "8"
          virtualHW.version = "{{ virtual_hw_version }}"
          virtualHW.productCompatibility = "hosted"

          displayName = "{{ vm_name }}"
          guestOS = "{{ guest_os }}"

          firmware = "efi"
          uefi.secureBoot.enabled = "FALSE"

          memsize = "{{ vm_memory_mb }}"
          numvcpus = "{{ vm_vcpu }}"
          cpuid.coresPerSocket = "1"

          # ==========================================================
          # NESTED VIRTUALIZATION
          # ==========================================================

          vhv.enable = "TRUE"
          hypervisor.cpuid.v0 = "FALSE"

          monitor.virtual_exec = "hardware"
          monitor.virtual_mmu = "hardware"

          # ==========================================================
          # PCIe BRIDGES
          # ==========================================================

          pciBridge0.present = "TRUE"
          pciBridge0.pciSlotNumber = "17"

          pciBridge1.present = "TRUE"
          pciBridge1.pciSlotNumber = "18"

          pciBridge2.present = "TRUE"
          pciBridge2.pciSlotNumber = "19"

          pciBridge3.present = "TRUE"
          pciBridge3.pciSlotNumber = "20"

          pciBridge4.present = "TRUE"
          pciBridge4.virtualDev = "pcieRootPort"
          pciBridge4.functions = "8"
          pciBridge4.pciSlotNumber = "21"

          pciBridge5.present = "TRUE"
          pciBridge5.virtualDev = "pcieRootPort"
          pciBridge5.functions = "8"
          pciBridge5.pciSlotNumber = "22"

          pciBridge6.present = "TRUE"
          pciBridge6.virtualDev = "pcieRootPort"
          pciBridge6.functions = "8"
          pciBridge6.pciSlotNumber = "23"

          pciBridge7.present = "TRUE"
          pciBridge7.virtualDev = "pcieRootPort"
          pciBridge7.functions = "8"
          pciBridge7.pciSlotNumber = "24"

          # ==========================================================
          # SCSI / INSTALLED ESXi DISK
          # ==========================================================

          scsi0.present = "TRUE"
          scsi0.virtualDev = "lsilogic"
          scsi0.pciSlotNumber = "160"

          scsi0:0.present = "TRUE"
          scsi0:0.fileName = "{{ vmdk_file_windows }}"
          scsi0:0.deviceType = "scsi-hardDisk"

          # ==========================================================
          # CD/DVD
          # ISO remains configured but disconnected
          # ==========================================================

          ide1:0.present = "TRUE"
          ide1:0.deviceType = "cdrom-image"
          ide1:0.fileName = "{{ esxi_iso_windows }}"
          ide1:0.startConnected = "FALSE"

          # ==========================================================
          # NETWORK - EXPLICIT VMnet0
          # ==========================================================

          ethernet0.present = "TRUE"
          ethernet0.connectionType = "custom"
          ethernet0.vnet = "VMnet0"
          ethernet0.virtualDev = "e1000e"
          ethernet0.addressType = "generated"
          ethernet0.startConnected = "TRUE"
          ethernet0.pciSlotNumber = "1184"

          # ==========================================================
          # DISABLE UNNECESSARY HARDWARE
          # ==========================================================

          usb.present = "FALSE"
          usb_xhci.present = "FALSE"
          ehci.present = "FALSE"

          sound.present = "FALSE"

          serial0.present = "FALSE"
          parallel0.present = "FALSE"
          floppy0.present = "FALSE"
          printer0.present = "FALSE"

          # ==========================================================
          # DISPLAY
          # ==========================================================

          svga.present = "TRUE"
          svga.autodetect = "TRUE"
          svga.vramSize = "134217728"

          # ==========================================================
          # NORMAL DISK BOOT
          # ==========================================================

          bios.bootDeviceClasses = "allow:all"

          # ==========================================================
          # POWER
          # ==========================================================

          powerType.powerOff = "soft"
          powerType.powerOn = "soft"
          powerType.suspend = "soft"
          powerType.reset = "soft"

          # ==========================================================
          # TOOLS
          # ==========================================================

          tools.upgrade.policy = "manual"
          tools.syncTime = "FALSE"

    # ========================================================================
    # VERIFY FINAL VMX
    # ========================================================================

    - name: Read final VMX
      ansible.builtin.slurp:
        src: "{{ vmx_file_cygwin }}"
      register: final_vmx_slurp

    - name: Set final VMX text
      ansible.builtin.set_fact:
        final_vmx_text: >-
          {{
            final_vmx_slurp.content
            | b64decode
          }}

    - name: Verify final VMX
      ansible.builtin.assert:
        that:
          - ('virtualHW.version = "' ~ virtual_hw_version ~ '"') in final_vmx_text
          - 'firmware = "efi"' in final_vmx_text
          - 'vhv.enable = "TRUE"' in final_vmx_text
          - 'ethernet0.connectionType = "custom"' in final_vmx_text
          - 'ethernet0.vnet = "VMnet0"' in final_vmx_text
          - 'scsi0:0.present = "TRUE"' in final_vmx_text
          - 'ide1:0.startConnected = "FALSE"' in final_vmx_text
        fail_msg: "Final installed ESXi VMX validation failed."
        success_msg: "Final installed ESXi VMX validated."

    - name: Display final VMX configuration
      ansible.builtin.debug:
        msg:
          - "============================================================"
          - "FINAL INSTALLED ESXi VMX"
          - "============================================================"
          - "Hardware Version : {{ virtual_hw_version }}"
          - "Firmware         : EFI"
          - "Disk             : {{ vmdk_file_windows }}"
          - "CD Connected     : FALSE"
          - "Network          : CUSTOM / VMnet0"
          - "Nested VT        : TRUE"
          - "Boot Mode        : NORMAL / DISK"
          - "============================================================"

    # ========================================================================
    # FINAL BOOT
    # ========================================================================

    - name: Remove VMware log before final boot
      ansible.builtin.file:
        path: "{{ vmware_log_cygwin }}"
        state: absent

    - name: Start installed ESXi VM
      ansible.builtin.command:
        argv:
          - powershell.exe
          - "-NoProfile"
          - "-NonInteractive"
          - "-Command"
          - >-
            Start-Process
            -FilePath "{{ vmware_windows }}"
            -ArgumentList @("-x","{{ vmx_file_windows }}")
      register: final_start
      changed_when: true
      failed_when: false

    - name: Display final boot launch
      ansible.builtin.debug:
        msg:
          - "============================================================"
          - "FINAL ESXi BOOT"
          - "============================================================"
          - "VM Name : {{ vm_name }}"
          - "Boot    : Installed VMDK"
          - "CD      : DISCONNECTED"
          - "Network : CUSTOM / VMnet0"
          - "============================================================"

    - name: Wait for installed ESXi to boot
      ansible.builtin.pause:
        seconds: "{{ vm_start_wait_seconds }}"

    # ========================================================================
    # VERIFY FINAL VM
    # ========================================================================

    - name: Get final running VMware VMs
      ansible.builtin.command:
        argv:
          - "{{ vmrun_cygwin }}"
          - "-T"
          - "ws"
          - "list"
      register: final_vmrun_list
      changed_when: false
      failed_when: false

    - name: Determine final VM state
      ansible.builtin.set_fact:
        final_vm_running: >-
          {{
            vmx_file_windows | lower
            in
            (final_vmrun_list.stdout | lower)
          }}

    - name: Display final VM state
      ansible.builtin.debug:
        msg:
          - "============================================================"
          - "FINAL ESXi VM STATUS"
          - "============================================================"
          - "VM Name : {{ vm_name }}"
          - "Running : {{ final_vm_running }}"
          - "VMX     : {{ vmx_file_windows }}"
          - "Network : CUSTOM / VMnet0"
          - "Boot    : INSTALLED DISK"
          - "ISO     : DISCONNECTED"
          - "============================================================"

    # ========================================================================
    # FAILURE LOG
    # ========================================================================

    - name: Check VMware log
      ansible.builtin.stat:
        path: "{{ vmware_log_cygwin }}"
      register: final_log_stat
      when: not final_vm_running | bool

    - name: Read VMware log on final boot failure
      ansible.builtin.shell: |
        tail -200 "{{ vmware_log_cygwin }}"
      args:
        executable: /bin/bash
      register: final_log_tail
      changed_when: false
      failed_when: false
      when:
        - not final_vm_running | bool
        - final_log_stat.stat.exists

    - name: Extract VMware final boot errors
      ansible.builtin.shell: |
        grep -iE \
        "error|failed|failure|pci|slot|ethernet|scsi|power on failed|unable to allocate|cancel|unsupported" \
        "{{ vmware_log_cygwin }}" |
        tail -100
      args:
        executable: /bin/bash
      register: final_log_errors
      changed_when: false
      failed_when: false
      when:
        - not final_vm_running | bool
        - final_log_stat.stat.exists

    # ========================================================================
    # FAIL IF FINAL VM DID NOT START
    # ========================================================================

    - name: Fail when installed ESXi VM is not running
      ansible.builtin.fail:
        msg: |
          ============================================================
          FINAL ESXi BOOT FAILED
          ============================================================

          VM:
          {{ vm_name }}

          VMX:
          {{ vmx_file_windows }}

          Final running state:
          {{ final_vm_running }}

          vmrun output:
          {{ final_vmrun_list.stdout | default('') }}

          ------------------------------------------------------------
          IMPORTANT VMWARE ERRORS
          ------------------------------------------------------------

          {{ final_log_errors.stdout | default('No filtered errors found.') }}

          ------------------------------------------------------------
          LAST VMWARE LOG
          ------------------------------------------------------------

          {{ final_log_tail.stdout | default('VMware log unavailable.') }}

          ============================================================
      when: not final_vm_running

    # ========================================================================
    # SUCCESS
    # ========================================================================

    - name: Display successful ESXi installation
      ansible.builtin.debug:
        msg:
          - "============================================================"
          - "SUCCESS - ESXi INSTALLATION COMPLETED"
          - "============================================================"
          - "VM Name        : {{ vm_name }}"
          - "VMX            : {{ vmx_file_windows }}"
          - "VMDK           : {{ vmdk_file_windows }}"
          - "Running        : YES"
          - "RAM            : {{ vm_memory_mb }} MB"
          - "CPU            : {{ vm_vcpu }} vCPU"
          - "Disk           : {{ vm_disk_gb }} GB"
          - "Firmware       : EFI"
          - "Nested VT      : ENABLED"
          - "Network        : CUSTOM / VMnet0"
          - "ISO            : E:\\vmware\\ESXi-8.0.1-auto.iso"
          - "ISO Connected  : NO"
          - "Boot           : INSTALLED ESXi DISK"
          - "============================================================"
      when: final_vm_running | bool
