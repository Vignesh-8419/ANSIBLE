---
# Play 1: Bootstrap Python 3.9
- name: Bootstrap Python 3.9 on Rocky 8
  hosts: all
  become: yes
  gather_facts: no

  tasks:
    - name: Install Python 3.9 if not present
      raw: |
        if ! command -v python3.9 >/dev/null 2>&1; then
          dnf install -y python39
        fi

    - name: Reset SSH connection
      meta: reset_connection


# Play 2: Configure HTTPS Repo Server
- name: Rocky 8 Repo Server → HTTPS + HTTP to HTTPS redirect
  hosts: all
  become: yes
  gather_facts: yes

  vars:
    ansible_python_interpreter: /usr/bin/python3.9

    fqdn: "http-server-01.vgs.com"
    ssl_key: "/etc/pki/tls/private/pvt.pem"
    ssl_cert: "/etc/pki/tls/certs/crt.pem"

  tasks:

    - name: Install required packages
      raw: |
        dnf install -y \
          httpd \
          mod_ssl \
          createrepo \
          firewalld \
          openssl \
          cifs-utils

    - name: Create repo directory
      file:
        path: /var/www/html/repo
        state: directory
        mode: '0777'

    - name: Mount CIFS share
      mount:
        path: /var/www/html/repo
        src: //192.168.31.87/ISO
        fstype: cifs
        opts: "username=vigne,password=Vigneshv12$,rw,dir_mode=0777,file_mode=0777,vers=3.0"
        state: mounted

    - name: Start and enable firewalld
      service:
        name: firewalld
        state: started
        enabled: yes

    - name: Allow HTTP and HTTPS through firewall
      firewalld:
        service: "{{ item }}"
        permanent: yes
        immediate: yes
        state: enabled
      loop:
        - http
        - https

    - name: Allow access to /var/www/html
      blockinfile:
        path: /etc/httpd/conf/httpd.conf
        marker: "# {mark} ANSIBLE MANAGED BLOCK - /var/www/html access"
        block: |
          <Directory "/var/www/html">
              Options Indexes FollowSymLinks
              AllowOverride None
              Require all granted
          </Directory>

    - name: Generate private key
      command: openssl genrsa -out {{ ssl_key }} 2048
      args:
        creates: "{{ ssl_key }}"

    - name: Generate self-signed certificate
      command: >
        openssl req -new -x509 -days 365
        -key {{ ssl_key }}
        -out {{ ssl_cert }}
        -subj "/C=IN/ST=Tamil Nadu/L=Chennai/O=VGS.COM/OU=VGS.COM/CN={{ fqdn }}"
      args:
        creates: "{{ ssl_cert }}"

    - name: Secure private key permissions
      file:
        path: "{{ ssl_key }}"
        mode: '0600'

    - name: Configure SSL VirtualHost
      blockinfile:
        path: /etc/httpd/conf.d/ssl.conf
        insertafter: "^<VirtualHost.*443>"
        marker: "# {mark} ANSIBLE MANAGED SSL CONFIG"
        block: |
          ServerName {{ fqdn }}
          SSLEngine on
          SSLCertificateFile {{ ssl_cert }}
          SSLCertificateKeyFile {{ ssl_key }}
      notify: restart httpd

    - name: Configure HTTP to HTTPS redirect
      copy:
        dest: /etc/httpd/conf.d/ssl-redirect.conf
        mode: '0644'
        content: |
          <VirtualHost *:80>
              ServerName {{ fqdn }}
              RewriteEngine On
              RewriteRule ^ https://%{SERVER_NAME}%{REQUEST_URI} [L,R=301]
          </VirtualHost>
      notify: restart httpd

    - name: Remove broken mod_rewrite symlink
      file:
        path: /etc/httpd/conf.modules.d/00-rewrite.conf
        state: absent

    - name: Start and enable Apache
      service:
        name: httpd
        state: started
        enabled: yes

    - name: Verify Apache configuration
      command: apachectl configtest
      register: apache_test
      changed_when: false

    - name: Test HTTP to HTTPS redirect
      uri:
        url: "http://{{ fqdn }}"
        follow_redirects: none
        validate_certs: no
      register: redirect_test
      delegate_to: localhost
      ignore_errors: true

    - name: Confirm redirect works
      assert:
        that:
          - redirect_test.status == 301
          - redirect_test.location is defined
          - "'https://' in redirect_test.location"
        success_msg: "SUCCESS → HTTP redirects to HTTPS ({{ redirect_test.location }})"
        fail_msg: "FAILED → HTTP redirect is not working"

  handlers:
    - name: restart httpd
      service:
        name: httpd
        state: restarted
