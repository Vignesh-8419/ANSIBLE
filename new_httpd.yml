---
# ============================================================================
# Play 1: Bootstrap Python 3.9
# ============================================================================

- name: Bootstrap Python 3.9 on Rocky 8
  hosts: all
  become: yes
  gather_facts: no

  tasks:
    - name: Install Python 3.9 if not present
      raw: |
        if ! command -v python3.9 >/dev/null 2>&1; then
          dnf install -y python39
        fi

    - name: Reset SSH connection
      meta: reset_connection


# ============================================================================
# Play 2: Configure HTTP Repository Server
# ============================================================================

- name: Configure Rocky 8 HTTP Repo Server
  hosts: all
  become: yes
  gather_facts: yes

  vars:
    ansible_python_interpreter: /usr/bin/python3.9

  tasks:

    - name: Install required packages
      raw: |
        dnf install -y \
          httpd \
          createrepo \
          firewalld \
          openssl \
          cifs-utils
      changed_when: false

    - name: Create repository directory
      file:
        path: /var/www/html/repo
        state: directory
        mode: '0777'

    - name: Mount CIFS share
      mount:
        path: /var/www/html/repo
        src: //192.168.31.87/ISO
        fstype: cifs
        opts: "username=vigne,password=Vigneshv12$,rw,dir_mode=0777,file_mode=0777,vers=3.0"
        state: mounted

    - name: Configure firewalld
      raw: |
        systemctl enable --now firewalld

        firewall-cmd --permanent --query-service=http \
          || firewall-cmd --permanent --add-service=http

        firewall-cmd --reload
      changed_when: false

    - name: Allow access to /var/www/html
      blockinfile:
        path: /etc/httpd/conf/httpd.conf
        marker: "# {mark} ANSIBLE MANAGED BLOCK - /var/www/html access"
        block: |
          <Directory "/var/www/html">
              Options Indexes FollowSymLinks
              AllowOverride None
              Require all granted
          </Directory>
      notify: restart httpd

    - name: Start and enable Apache
      service:
        name: httpd
        state: started
        enabled: yes

    - name: Apply Apache changes
      meta: flush_handlers

    - name: Verify Apache configuration
      command: apachectl configtest
      register: apache_test
      changed_when: false

    - name: Verify HTTP access locally
      command: curl -I http://127.0.0.1
      register: http_test
      changed_when: false

    - name: Confirm HTTP access works
      assert:
        that:
          - "'200 OK' in http_test.stdout"
        success_msg: "SUCCESS → HTTP web server is accessible."
        fail_msg: "FAILED → HTTP web server is not responding correctly."

  handlers:
    - name: restart httpd
      service:
        name: httpd
        state: restarted
