---
- name: Prepare and recreate Rocky 8 hosts in Foreman using API
  hosts: localhost
  connection: local
  gather_facts: no

  collections:
    - community.vmware

  vars:

    ###########################################################################
    # NETBOX
    ###########################################################################

    netbox_url: "https://netbox.vgs.com"
    netbox_token: "83fb0cec1adff8ff4f36c9185df6b9e2f07c7fcd"


    ###########################################################################
    # FOREMAN SERVER SELECTION
    #
    # AWX Survey:
    #
    # 1 = Frontend
    # 2 = Backend
    #
    ###########################################################################

    foreman_server: "{{ foreman_server | default('1', true) }}"

    foreman_url: >-
      {{
        {
          '1': 'https://rocky-08-01.vgs.com',
          '2': 'https://cent-07-01.vgs.com'
        }[foreman_server | string]
      }}

    foreman_user: "admin"
    foreman_api_token: "_bkE_ov7gKb68d3aR01m5A"


    ###########################################################################
    # HOST SETTINGS
    ###########################################################################

    host_root_pass: "Root@123"

    cluster_name: "rocky-8-servers"

    # Example:
    #
    # rocky-08-01,rocky-08-02
    #
    # Leave empty to process all hosts.
    #
    target_hosts: "{{ target_hosts | default('', true) }}"

    target_servers: >-
      {{
        target_hosts.split(',')
        | map('trim')
        | reject('equalto', '')
        | list
      }}


    ###########################################################################
    # FOREMAN ORGANIZATION / LOCATION
    ###########################################################################

    org_id: 1
    loc_id: 2


    ###########################################################################
    # ROCKY LINUX 8.10 HOSTGROUP SELECTION
    #
    # 1 = RockyLinux8.10-SingleDisk
    # 2 = RockyLinux8.10-RAID
    ###########################################################################

    hostgroup: "{{ hostgroup | default('1', true) }}"

    hostgroup_id: >-
      {{
        {
          '1': 6,
          '2': 2
        }[hostgroup | string]
      }}


    ###########################################################################
    # FOREMAN PROVISIONING IDS
    ###########################################################################

    operatingsystem_id: 4
    medium_id: 16
    ptable_id: 126

    subnet_name: "vgs-subnet-rockyos"

    content_view_id: 3
    lifecycle_environment_id: 1

    architecture_id: 1
    domain_id: 1

    pxe_loader: "Grub2 UEFI"
    build: true


  tasks:

    ###########################################################################
    # SHOW CONFIGURATION
    ###########################################################################

    - name: Show selected Rocky 8 provisioning configuration
      debug:
        msg:
          - "============================================================"
          - "ROCKY LINUX 8 FOREMAN PROVISIONING"
          - "============================================================"
          - "Foreman Server        : {{ foreman_url }}"
          - "Cluster               : {{ cluster_name }}"
          - "Hostgroup Selection   : {{ hostgroup }}"
          - "Hostgroup ID          : {{ hostgroup_id }}"
          - "Operating System ID   : {{ operatingsystem_id }}"
          - "Medium ID             : {{ medium_id }}"
          - "Partition Table ID    : {{ ptable_id }}"
          - "Subnet Name           : {{ subnet_name }}"
          - "Content View ID       : {{ content_view_id }}"
          - "Lifecycle Environment : {{ lifecycle_environment_id }}"
          - "Architecture ID       : {{ architecture_id }}"
          - "Domain ID             : {{ domain_id }}"
          - "PXE Loader            : {{ pxe_loader }}"
          - "============================================================"


    ###########################################################################
    # GET DEVICES FROM NETBOX
    ###########################################################################

    - name: Get devices from NetBox cluster
      uri:
        url: "{{ netbox_url }}/api/dcim/devices/?cluster={{ cluster_name | urlencode }}&limit=1000"
        method: GET
        headers:
          Authorization: "Token {{ netbox_token }}"
        validate_certs: no
        use_proxy: false
        return_content: yes
        status_code: 200
      register: netbox_devices


    - name: Show devices returned by NetBox
      debug:
        msg: "{{ netbox_devices.json.results | map(attribute='name') | list }}"


    ###########################################################################
    # FILTER SELECTED HOSTS
    ###########################################################################

    - name: Initialize filtered device list
      set_fact:
        filtered_devices: []


    - name: Use all devices when target_hosts is empty
      set_fact:
        filtered_devices: "{{ netbox_devices.json.results }}"
      when: target_servers | length == 0


    - name: Filter devices by hostname when target_hosts is specified
      set_fact:
        filtered_devices: "{{ filtered_devices + [item] }}"
      loop: "{{ netbox_devices.json.results }}"
      when:
        - target_servers | length > 0
        - item.name.split('.')[0] in target_servers


    - name: Show filtered devices
      debug:
        msg: "{{ filtered_devices | map(attribute='name') | list }}"


    - name: Fail if no hosts matched
      fail:
        msg: "No matching hosts found in NetBox."
      when: filtered_devices | length == 0


    ###########################################################################
    # GET VM INFORMATION FROM ESXI
    ###########################################################################

    - name: Get VM information from ESXi
      community.vmware.vmware_guest_info:
        hostname: "192.168.253.128"
        username: "root"
        password: "Root@123"
        validate_certs: false
        name: "{{ item.name.split('.')[0] }}"
      loop: "{{ filtered_devices }}"
      loop_control:
        label: "{{ item.name }}"
      register: vm_info


    ###########################################################################
    # GET IP ADDRESSES FROM NETBOX
    ###########################################################################

    - name: Get IP addresses from NetBox
      uri:
        url: "{{ netbox_url }}/api/ipam/ip-addresses/?limit=1000"
        method: GET
        headers:
          Authorization: "Token {{ netbox_token }}"
        validate_certs: no
        use_proxy: false
        return_content: yes
        status_code: 200
      register: netbox_ips


    ###########################################################################
    # BUILD HOST INFORMATION
    ###########################################################################

    - name: Initialize host information
      set_fact:
        host_info: []


    - name: Build host information
      set_fact:
        host_info: "{{ host_info + [ {
          'name': item.0.name.split('.')[0],
          'netbox_name': item.0.name,
          'ip': (
            netbox_ips.json.results
            | selectattr('assigned_object', 'defined')
            | selectattr('assigned_object', 'ne', none)
            | selectattr('assigned_object.device', 'defined')
            | selectattr('assigned_object.device', 'ne', none)
            | selectattr('assigned_object.device.name', 'equalto', item.0.name)
            | map(attribute='address')
            | map('regex_replace', '/.*$', '')
            | first
            | default('')
          ),
          'mac': (
            item.1.instance.hw_eth0.macaddress
            | default('')
          )
        } ] }}"
      loop: "{{ filtered_devices | zip(vm_info.results) | list }}"
      loop_control:
        label: "{{ item.0.name }}"


    - name: Show host information
      debug:
        var: host_info


    - name: Verify host IP addresses were found
      fail:
        msg: "No IP address found in NetBox for {{ item.name }}"
      loop: "{{ host_info }}"
      when: item.ip | length == 0


    - name: Verify host MAC addresses were found
      fail:
        msg: "No MAC address found from ESXi for {{ item.name }}"
      loop: "{{ host_info }}"
      when: item.mac | length == 0


    ###########################################################################
    # RESOLVE FOREMAN SUBNET
    ###########################################################################

    - name: Get Foreman subnet
      uri:
        url: "{{ foreman_url }}/api/subnets?search=name={{ subnet_name | urlencode }}"
        method: GET
        user: "{{ foreman_user }}"
        password: "{{ foreman_api_token }}"
        force_basic_auth: yes
        validate_certs: no
        use_proxy: false
        timeout: 120
        status_code: 200
        return_content: yes
      register: foreman_subnets


    - name: Resolve Foreman subnet ID
      set_fact:
        subnet_id: >-
          {{
            foreman_subnets.json.results
            | selectattr('name', 'equalto', subnet_name)
            | map(attribute='id')
            | first
            | default('')
          }}


    - name: Verify Foreman subnet
      fail:
        msg: "Foreman subnet not found: {{ subnet_name }}"
      when: subnet_id | string | length == 0


    - name: Show Foreman subnet
      debug:
        msg:
          - "Subnet Name : {{ subnet_name }}"
          - "Subnet ID   : {{ subnet_id }}"


    ###########################################################################
    # SEARCH FOREMAN HOSTS BY NAME
    #
    # IMPORTANT:
    # Do NOT use /api/hosts/{{ hostname }}.
    #
    # Foreman treats this value as an ID in some cases.
    #
    # Search by name and get the real numeric Foreman ID.
    ###########################################################################

    - name: Search existing Foreman hosts by hostname
      uri:
        url: "{{ foreman_url }}/api/hosts?search=name={{ item.name | urlencode }}"
        method: GET
        user: "{{ foreman_user }}"
        password: "{{ foreman_api_token }}"
        force_basic_auth: yes
        validate_certs: no
        use_proxy: false
        timeout: 120
        status_code: 200
        return_content: yes
      loop: "{{ host_info }}"
      loop_control:
        label: "{{ item.name }}"
      register: foreman_host_search


    - name: Build existing Foreman host information
      set_fact:
        existing_foreman_hosts: "{{ existing_foreman_hosts | default([]) + [ {
          'name': item.item.name,
          'found': (
            item.json.results
            | selectattr('name', 'equalto', item.item.name)
            | list
            | length > 0
          ),
          'id': (
            item.json.results
            | selectattr('name', 'equalto', item.item.name)
            | map(attribute='id')
            | first
            | default('')
          )
        } ] }}"
      loop: "{{ foreman_host_search.results }}"
      loop_control:
        label: "{{ item.item.name }}"


    - name: Show existing Foreman host status
      debug:
        msg:
          - "Host  : {{ item.name }}"
          - "Found : {{ item.found }}"
          - "ID    : {{ item.id | default('NOT FOUND') }}"
          - "Action: {{ 'DELETE AND RECREATE' if item.found else 'CREATE NEW' }}"
      loop: "{{ existing_foreman_hosts }}"
      loop_control:
        label: "{{ item.name }}"


    ###########################################################################
    # DELETE EXISTING FOREMAN HOSTS USING NUMERIC ID
    ###########################################################################

    - name: Delete existing Foreman host by ID
      uri:
        url: "{{ foreman_url }}/api/hosts/{{ item.id }}"
        method: DELETE
        user: "{{ foreman_user }}"
        password: "{{ foreman_api_token }}"
        force_basic_auth: yes
        validate_certs: no
        use_proxy: false
        timeout: 120
        status_code:
          - 200
          - 202
          - 204
      loop: "{{ existing_foreman_hosts }}"
      loop_control:
        label: "{{ item.name }}"
      when: item.found | bool
      register: foreman_delete


    - name: Show deleted Foreman hosts
      debug:
        msg: "Deleted existing Foreman host: {{ item.item.name }}"
      loop: "{{ foreman_delete.results | default([]) }}"
      loop_control:
        label: "{{ item.item.name }}"
      when:
        - item.skipped is not defined or not item.skipped
        - item.status is defined


    ###########################################################################
    # WAIT UNTIL HOST NAME NO LONGER EXISTS
    ###########################################################################

    - name: Wait until existing Foreman host deletion is complete
      uri:
        url: "{{ foreman_url }}/api/hosts?search=name={{ item.name | urlencode }}"
        method: GET
        user: "{{ foreman_user }}"
        password: "{{ foreman_api_token }}"
        force_basic_auth: yes
        validate_certs: no
        use_proxy: false
        timeout: 120
        status_code: 200
        return_content: yes
      register: deletion_check
      until: >-
        (
          deletion_check.json.results
          | selectattr('name', 'equalto', item.name)
          | list
          | length
        ) == 0
      retries: 30
      delay: 2
      loop: "{{ existing_foreman_hosts }}"
      loop_control:
        label: "{{ item.name }}"
      when: item.found | bool


    ###########################################################################
    # FINAL SAFETY CHECK
    #
    # Prevents:
    #
    # Name has already been taken
    ###########################################################################

    - name: Perform final Foreman hostname availability check
      uri:
        url: "{{ foreman_url }}/api/hosts?search=name={{ item.name | urlencode }}"
        method: GET
        user: "{{ foreman_user }}"
        password: "{{ foreman_api_token }}"
        force_basic_auth: yes
        validate_certs: no
        use_proxy: false
        timeout: 120
        status_code: 200
        return_content: yes
      loop: "{{ host_info }}"
      loop_control:
        label: "{{ item.name }}"
      register: final_name_check


    - name: Fail if Foreman hostname still exists
      fail:
        msg: >-
          Foreman hostname still exists and cannot be recreated:
          {{ item.item.name }}
      when: >-
        (
          item.json.results
          | selectattr('name', 'equalto', item.item.name)
          | list
          | length
        ) > 0
      loop: "{{ final_name_check.results }}"
      loop_control:
        label: "{{ item.item.name }}"


    ###########################################################################
    # CREATE HOSTS
    ###########################################################################

    - name: Create Rocky 8 host in Foreman
      uri:
        url: "{{ foreman_url }}/api/hosts"
        method: POST
        user: "{{ foreman_user }}"
        password: "{{ foreman_api_token }}"
        force_basic_auth: yes
        validate_certs: no
        use_proxy: false
        timeout: 120

        status_code:
          - 200
          - 201

        headers:
          Content-Type: application/json

        body_format: json

        body:
          host:

            name: "{{ item.name }}"

            organization_id: "{{ org_id }}"
            location_id: "{{ loc_id }}"

            hostgroup_id: "{{ hostgroup_id }}"
            operatingsystem_id: "{{ operatingsystem_id }}"
            medium_id: "{{ medium_id }}"
            ptable_id: "{{ ptable_id }}"
            architecture_id: "{{ architecture_id }}"

            subnet_id: "{{ subnet_id }}"
            domain_id: "{{ domain_id }}"

            build: "{{ build }}"
            pxe_loader: "{{ pxe_loader }}"
            root_pass: "{{ host_root_pass }}"

            content_facet_attributes:
              content_view_id: "{{ content_view_id }}"
              lifecycle_environment_id: "{{ lifecycle_environment_id }}"

            interfaces_attributes:
              - type: interface
                mac: "{{ item.mac }}"
                ip: "{{ item.ip }}"
                subnet_id: "{{ subnet_id }}"
                domain_id: "{{ domain_id }}"
                primary: true
                provision: true
                managed: true

      loop: "{{ host_info }}"
      loop_control:
        label: "{{ item.name }}"
      register: foreman_create


    ###########################################################################
    # SHOW CREATE RESULTS
    ###########################################################################

    - name: Show Foreman create result
      debug:
        msg:
          - "============================================================"
          - "Host    : {{ item.item.name }}"
          - "Status  : {{ item.status }}"
          - "Host ID : {{ item.json.id | default('UNKNOWN') }}"
          - "============================================================"
      loop: "{{ foreman_create.results }}"
      loop_control:
        label: "{{ item.item.name }}"


    ###########################################################################
    # VERIFY USING NUMERIC ID RETURNED BY POST
    #
    # This is the key fix.
    ###########################################################################

    - name: Refresh Foreman host information using created ID
      uri:
        url: "{{ foreman_url }}/api/hosts/{{ item.json.id }}"
        method: GET
        user: "{{ foreman_user }}"
        password: "{{ foreman_api_token }}"
        force_basic_auth: yes
        validate_certs: no
        use_proxy: false
        timeout: 120
        status_code: 200
        return_content: yes
      loop: "{{ foreman_create.results }}"
      loop_control:
        label: "{{ item.item.name }}"
      register: final_hosts


    ###########################################################################
    # SHOW FINAL CONFIGURATION
    ###########################################################################

    - name: Show final host configuration
      debug:
        msg:
          - "============================================================"
          - "Host       : {{ item.json.name }}"
          - "Host ID    : {{ item.json.id }}"
          - "Host Group : {{ item.json.hostgroup_name | default('N/A') }}"
          - "OS         : {{ item.json.operatingsystem_name | default('N/A') }}"
          - "Medium     : {{ item.json.medium_name | default('N/A') }}"
          - "Subnet     : {{ item.json.subnet_name | default('N/A') }}"
          - "IP         : {{ item.json.ip | default('N/A') }}"
          - "PXE Loader : {{ item.json.pxe_loader | default('N/A') }}"
          - "Build      : {{ item.json.build | default('N/A') }}"
          - "Status     : {{ item.json.build_status_label | default('N/A') }}"
          - "============================================================"
      loop: "{{ final_hosts.results }}"
      loop_control:
        label: "{{ item.json.name }}"


    ###########################################################################
    # FINAL SUCCESS MESSAGE
    ###########################################################################

    - name: Show completion message
      debug:
        msg:
          - "============================================================"
          - "ROCKY LINUX 8 FOREMAN RECREATION COMPLETED SUCCESSFULLY"
          - "============================================================"
          - "Hosts Processed       : {{ host_info | length }}"
          - "Existing hosts        : {{ existing_foreman_hosts | selectattr('found') | list | length }}"
          - "New hosts created     : {{ foreman_create.results | length }}"
          - "PXE build mode        : {{ build }}"
          - "============================================================"
